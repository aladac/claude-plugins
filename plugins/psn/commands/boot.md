---
description: Play the PSN HUD bootup animation sequence
allowed-tools:
  - Bash
---

# HUD Boot

Run the PSN HUD bootup animation with audio.

## Execution

```bash
marauder hud boot
```

If the HUD bridge is not running, tell the user to start it first with `cd ~/Projects/marauder-visor && just dev`.
